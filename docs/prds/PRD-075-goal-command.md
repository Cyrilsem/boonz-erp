# PRD-075 goal command

GOAL: Execute PRD-075 (docs/prds/PRD-075-priority-chapter-close.md) end to end, AUTO mode. Self-run Dara/Cody/Stax. Apply only green pieces, SKIP-and-HIGHLIGHT gate failures, keep PRD-075-EXECUTION-LOG.md current, never wait on CS.

HARD GATES: engine_add_pod, engine_swap_pod, pick_machines_for_refill md5 byte-identical. WS-C changes v_machine_priority by COLUMN EXPOSURE ONLY: prove p_tier and urgency byte-identical for every machine before vs after (full-fleet diff in the log); record the expected view md5 change in MIGRATIONS_REGISTRY. WS-A changes v_live_shelf_stock: full-fleet before/after eligibility diff must show ONLY ACTIVATE-2005-0000-W0, IFLYMCC-1024-0000-W0, MPMCC-1054-0000-M0 flipping. BEGIN..ROLLBACK before every write. New knobs in pick_urgency_params. Build green before merge. Migrations forward-only, _HELD rollback bodies kept.

CONTEXT: CS rulings 2026-07-04: (1) un-blind the 3 repurposed-but-Active machines WITHOUT touching repurposed_at (chat NULL attempt correctly rejected by chk_repurpose_consistency; repurposed_at + previous_location = permanent relocation history, ACTIVATE-2005 was MPMCC-2005); (2) logged manual/field refills count as canonical visit evidence; (3) split the lumped core chip.

WS-A repurpose grace: v_live_shelf_stock is_eligible_machine adds (repurposed_at IS NULL OR repurposed_at < now() - make_interval(days => pick_urgency_params.repurpose_grace_days)), new param default 30. Cody enumerates all consumers of v_live_shelf_stock and confirms behavior changes only for the repurposed-old case. T: the 3 machines gain nonzero grades in v_machine_priority; v_machine_eligibility_drift = zero rows; MPMCC-1058 stays excluded (Pending Setup, legitimate).

WS-B manual refills = visits: find the log_manual_refill event table (PRD-036/040 lineage). v_machine_health_signals last_visit = GREATEST(executed dispatch evidence (unchanged), latest manual refill event). Verify get_machine_health v3 inherits (pass-through, no cached copy). Update the METRICS_REGISTRY days_since_visit row: executed dispatch OR logged manual refill. FieldCapturePanel minimal hardening only: machine-scoped product list (pods on that machine's shelves, not all-300), default qty fill-to-cap, offline-tolerant submit; no redesign. T: rolled-back txn inserts a test manual-refill row for a VOX machine and days_since_visit resets inside the txn; check_priority_surface_consistency() still zero diffs.

WS-C core chip split: add s_runout, s_capacity, s_expiry, s_stale as v_machine_priority output columns (values already computed in mscore; exposure only). get_machine_health urgency_breakdown replaces the lumped core entry with the four real terms (weights applied); chip sum must equal urgency exactly, fleet-wide. FE renders the richer list verbatim, zero new math. Extend check_priority_surface_consistency() to the four fields.

CLOSE: registries (MIGRATIONS/RPC/CHANGELOG/METRICS_REGISTRY), PRD-075 and PRD-073/074 status lines (their carry-forwards close here), execution log, commit and push, main == origin/main. Final report: the three fleet-diff proofs (eligibility flip list, tier/urgency invariance, chip-sum check), FieldCapturePanel change summary, T-test table, anything skipped and why.
