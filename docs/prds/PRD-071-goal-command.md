# PRD-071 goal command

GOAL: Execute PRD-071 (docs/prds/PRD-071-environment-closeout-one-shot.md) end to end, AUTO mode. Self-run Dara/Cody. Apply only green pieces. SKIP-and-HIGHLIGHT gate failures. Keep PRD-071-EXECUTION-LOG.md current throughout. Never wait on CS.

HARD GATES (all workstreams): engine_add_pod and engine_swap_pod md5 byte-identical before and after. swaps_enabled stays false. Migrations forward-only. Every write proven with BEGIN..ROLLBACK dry-run first. Writers idempotent. Never -D a branch, never force-push, never delete a branch carrying code not proven on main or prod.

WS-A branch reset (do first): You are likely on feat/prd-065-field-reconciliation (merged, 274 behind, 83 dirty files). Classify dirty files: keep docs/skills/registries that differ from origin/main, discard generated junk (list the manifest). Commit keepers to chore/prd-071-wip-salvage cut FROM origin/main. Then fetch, checkout main, pull --ff-only, git branch -d feat/prd-065-field-reconciliation (if -d refuses, STOP and highlight). Gate: clean tree, main == origin/main.

WS-B auto-wire M2M pairing (CS approved apply-with-gates): Wire pair_internal_transfer_m2m into push_plan_to_dispatch, post-insert, same txn, idempotent; pairing failure logs and continues, never fails the push. Dara designs, Cody reviews (refill_dispatching is protected; name articles). Prove on a real plan date in BEGIN..ROLLBACK: transfer legs get is_m2m + m2m_transfer_id, WH delta = 0, non-transfer rows unchanged. Run twice for idempotency. All green: apply, then backfill unpaired legs since 2026-07-02 with the same proof. Any gate fails: leave the migration authored and highlight.

WS-C data anomalies (CS approved defaults). For each: read state, snapshot rows into the log, dry-run, commit. 1) Cancel the two MINDSHARE-1009 stale Remove legs (2026-05-20, qty 3 and 5, internal_transfer, is_m2m false, no dest). 2) Transfer 1538f35f: approve via approve_m2m_transfer; if rejected on returned=true or past date, clear returned and re-date, then approve; WH delta must be 0. 3) Convert anomaly: add the fix at the APPROVE path (normalize returned/date on approval), convert untouched.

WS-D backlog truth sweep: For every PRD 023..070, fuse truth from execution logs, MIGRATIONS_REGISTRY, RPC_REGISTRY, CHANGELOG, origin/main log, and read-only prod checks where a migration is claimed. Rewrite each status line to Shipped / Applied (name missing commit) / Draft / Closed. Fix at minimum 037, 042, 043, 044, 045, 046, 048, 054, 063, 068 (live but shown Draft). Batch-close stale drafts (024, 025, 026, 027, 030, 031, 034, 035, 036, 039 and any other 14+ days untouched) with: "Status: Closed 2026-07-02 (PRD-071 sweep). Reason: <superseded by / overtaken by>. Reopen by deleting this line." Keep 061, 062, 064, 066, 067, 069 open, add a one-line verdict each. Append any missing registry entries found.

WS-E git salvage and prune (CS approved salvage-then-delete): For branches prd-020-packing-partial, prd-028-metrics-registry, prd-033-operator-flexibility, prd-047-v2, all four prd-049, prd-050, prd-052-convert-m2m, prd-053-stitch-conservation, products-performance-table, wip/realign-2026-06-16: list ahead commits; cherry-pick docs/log/registry commits onto docs/prd-071-salvage cut from main; for code commits verify the change exists on main or in prod (diff function bodies) and record the verdict. Any code not covered: KEEP the branch, highlight. Merge both salvage branches to main, push, then delete fully-covered branches locally (-d) and list remote deletes performed. Finish with bash boonz_git_cleanup.sh --apply from the BOONZ BRAIN parent and paste output.

WS-F close: Regenerate the monitor (python3 boonz_build_refresh.py from BOONZ BRAIN parent); banner must be GREEN and open set must equal 061, 062, 064, 066, 067, 069 plus any highlights. Commit all docs and migrations to main, push (git must reconstruct prod, PRD-057 rule). Final report: shipped, skipped and why, new open list.
