/goal Ship PRD-119b in ONE loop, end to end, to completion — the first-week fixes to PRD-119 (expiry management) found in production 04–07 Sep 2026, plus the three items the PRD-119 loop held. Repo: ~/Documents/Boonz Script and Data/BOONZ BRAIN/boonz-erp. Supabase eizcexopcuoycuosittm. Deploy through the existing Vercel pipeline from main.

READ FIRST, IN FULL: docs/prds/PRD-119-expiry-management-and-smart-inventory.md (the spec — its §1 fence, §3 decisions and §4 driver line still govern), then docs/prds/PRD-119-REPORT.md (what shipped 04 Sep and what was held), then docs/prds/PRD-120-pack-and-identity-integrity.md (shipped 07 Sep on main faefcaa: L1 pack-screen Remove/Refill merge fix, L2 substitute_dispatch_line supersede-and-relink = the old item L, quarantine reader audit that closed G2b), then this file's EVIDENCE section. Build on PRD-120, do not redo it. Registries as before: RPC_REGISTRY, MIGRATIONS_REGISTRY, METRICS_REGISTRY, CHANGELOG — update all four.

NON-NEGOTIABLE RULES — unchanged from PRD-119-goal-command.md: dara before schema, cody before every migration, canonical RPCs only, SECURITY DEFINER + role check + reason, migration file committed immediately, branch prd-119b, merge to main only with tsc green + fixtures green, Co-Authored-By trailer. Do not touch live plan/dispatch rows for the current plan_date or tomorrow's; reconciliation of this week's physical removals goes through the WM confirmation queue (lines created, WM confirms), never through direct inventory writes. Packing keeps the system recommendation. Engine owns pulls; 119 emits only CHECK and REMOVE EXPIRED.

EVIDENCE — what broke in the first week (verified in the live DB 07 Sep)

E1. Engine Remove legs for expiring shelf lots carry the WRONG expiry, taken from warehouse FEFO instead of the shelf lot. VOXMCC-1005 "Vitamin Well Zero Lemon ×3, exp 06-09 — pull" was written as a Remove leg on A15 with expiry_date 2027-12-03 and the comment "[EXPIRY-TO-CONFIRM — remainder not attributable to a known batch]" — the shelf lot is on A16, dated 2026-09-06. VOXMCC-1011 A11 "Nutella ×2 (the 09-09 units)" Remove leg stamped 2026-12-13 (the warehouse batch), shelf lot is 2026-09-09. Same on VOXMCC-1011 A15 (VW Zero Lemon, lot is on A10). The Remove leg's date and shelf were resolved against warehouse_inventory batches; a Remove of shelf stock must resolve against pod_inventory lots for that machine + shelf + product. This is CS's "not logged as removed because of FEFO".
E2. No Remove leg dispatched 04–07 Sep has a driver_outcome, a returned flag, or a disposition_events row (20 legs, 0 outcomes). The driver's "done" on a Remove line writes nothing to the lot ledger and creates no WM Confirmations line. The only removal that reached the ledger this week is one Leibniz tap through the P3 category (MPMCC-1054, 05 Sep). Result: lots the team physically pulled are still Active: VOXMCC-1005 A16 VW Zero Lemon 3u (06-09), VOXMCC-1011 A10 VW Zero Lemon 3u (06-09), VOXMCC-1011 A11 Nutella 2u (09-09), IRIS A01 Activia H&O 2u (05-09, pulled today).
E3. Packing screen blocked by Remove legs (05 Sep, VOXMCC-1011 "0795"): Jojo could not "save packing" for ~2 hours because the machine's list held a Remove line with nothing to pack; he asked "is this for refill or remove from machine?". Remove legs must never gate Finish on the packing screen, must be visually separated ("DRIVER: remove from machine — nothing to pack"), and must auto-carry pack_outcome='no_pack_needed'.
E4. Physical removals not in the ledger at all: 0715 2× Be-Kind Peanut Butter exp 02-09-26 ("was it in the system?" — "no"); VOXMCC-1011 1× Barebells White Almond expired; 11× Vitamin Well Lemon exp 06-09 removed from "0736"; NISSAN 04 Sep 2× Bounty expired + 2× Activia Honey (exp 05-09); AMZ "0745" 1× Activia. All reported on WhatsApp, none recorded. (Device numbers 0715/0736/0745 → resolve via the device-number derivation reference in memory / machines table.)
E5. Drawer + driver line label an expired lot with the lane's CURRENT WEIMI product: VOXMCC-1005 A16 shows "Aquafina — EXPIRED 3" while the lot is VW Zero Lemon 06-09 (Aquafina is venue-sourced, always 2099-12-31, can never expire).
E6. When a lane's WEIMI product changes and no Remove was recorded, the previous product's lots stay Active on the shelf forever (A16 above; the Sun Blast/Skittles/Tamreem mix on VOXMCC-1011 A15). Nothing surfaces them.
E7. The P3 driver category fired once in 4 days across ~15 machine visits that each had expired/≤3d lots. Determine why: not rendered when an engine Remove leg exists for the same lot? category collapsed? machines opened from a different screen? Fix so the category is unmissable when non-empty (count badge on the machine card, auto-expanded, blocks "Done dispatched" until each red row is answered).

WORK IN ORDER

T1 — Remove legs resolve against the SHELF LOT (E1). Any Remove/pull leg for shelf stock (engine expiry pull, CS-dictated remove, swap remove) takes shelf_id, expiry_date and the lot reference (pod_inventory_id) from the pod lot for that machine+product with the matching/earliest expiry — never from warehouse_inventory, never "EXPIRY-TO-CONFIRM" when a dated lot exists. Add pod_lot_id (uuid, FK pod_inventory) on refill_dispatching. Repair the 04–07 Sep Remove legs listed in E1 in place (re-point shelf + expiry + pod_lot_id) with an audited RPC; do not recreate them.
T2 — Driver "done" on a Remove leg = the lot write + the WM line (E2). driver_outcome on a Remove leg (done / partial with driver_outcome_qty / not_there) writes exactly what the P3 tap writes: lot current_stock −= n (archive at 0, removal_reason expired_writeoff or removed_<reason>), disposition_events removed_at_machine, one v_wm_confirmations line pre-filled. Not-there → archive sold_through, no line. One writer for both paths (the P3 tap and the Remove leg), so there is one code path, not two.
T3 — Packing screen (E3): PRD-120 L1 already stopped the Remove/Refill merge. Verify against E3 and add only what is missing: Remove legs render in their own block under the pack list, greyed, "Driver removes at machine — nothing to pack", pack_outcome forced no_pack_needed, never counted in the Finish gate. Load `stax`. Fixture: a machine with only a Remove leg → Finish enabled immediately.
T4 — Lot identity in every expiry surface (E5): get_machine_slots_with_expiry, the /refill drawer, the field category and the WM queue show the LOT's product + date, with "lane now: <WEIMI product>" when they differ. Never the lane product on an expiry row.
T5 — Orphan lots when the lane changes product (E6): nightly + on-visit: any Active lot whose boonz product is not the lane's current WEIMI product (and not a variant of the same pod product) becomes a CHECK row in the driver category ("was <product> <date> still here? Removed / Not there") and shows as "orphan lot" in the drawer. Fixture: VOXMCC-1005 A16.
T6 — Driver category unmissable (E7): root-cause first, then fix; add the count badge on the machine card and the "answer red rows before Done dispatched" gate. Verify on the field PWA with a synthetic machine.
T7 — Reconcile this week (E4 + E2): create WM Confirmations lines, pre-filled, for every physical removal listed in E2/E4 that has no record (product, qty, date, machine, source='reconcile_2026-09-07', proposed outcome waste); the WM confirms what she physically has. List every line created in the report. Nothing written to inventory by the loop itself.
T8 — The held PRD-119 item still open (item L and G2b were closed by PRD-120 / the close-out audit — do not redo): the 320 stale-line sweep — verify the predicate (packed AND picked_up AND driver_outcome IS NULL AND NOT item_added AND NOT returned AND dispatch_date < today − 5), dry-run, print, apply as driver_outcome='delivered_unconfirmed', release consumer_stock, list in the CS summary. Then re-run the three nightly guards the close-out audit scheduled and report the deltas: expiry_unvalidated (175 at audit — expected to fall only through T5/T6 driver CHECK rows, NOT through a cleanup), sentinel-bound dispatch rows (55 — classify: vox_at_venue / venue_team lines are legitimate, fix the assertion to exclude them; anything else re-pin via repin_dispatch_batch), over-committed batches (54 — expected to fall after the sweep; list what remains). No blind data fixes.

OUT OF SCOPE (decided by CS 07 Sep): D4 pull-horizon table does not exist by DECISION (no category thresholds) — mark it CLOSED-BY-DECISION in the PRD-119 ledger, not 'not shipped'. D3 receipt-capture UX stays deferred. The three functions still matching raw sales_history names (get_machine_health, auto_generate_refill_plan, get_machine_slots_with_expiry) are a separate follow-up; in this loop only CHECK whether auto_generate_refill_plan is actually invoked anywhere (cron, n8n, FE) and report it.

STOP CONDITIONS — as PRD-119: stop and write up anything touching live rows of the current/next plan_date beyond the E1 leg repair and T7 queue lines, any Cody FAIL after two attempts, any device-number → machine mapping you cannot resolve (list it, do not guess).

REPORT — docs/prds/PRD-119b-REPORT.md: per item what shipped, Cody verdict, fixtures; the E1 legs repaired (before/after shelf + expiry); the T7 lines created (machine, product, qty, date); the 320-sweep result; the E7 root cause in one paragraph; end with `## PRD-119b DONE` only when T1–T8 are green and deployed.
