# /goal — PRD-018 (paste into Claude Code, repo root)

```
/goal Execute docs/prds/PRD-018-jun03-04-refills-and-dispatch-bugs.md on Supabase eizcexopcuoycuosittm. Cody review BEFORE any canonical-writer/view/trigger change; reproduce bodies verbatim; do NOT redo PRD-017 §0.

PART 1 — LOG PLACEMENTS exactly as listed in PRD-018 Part 1 (04-Jun NISSAN/MC-2004/OMDCW/NOVO manual refill = primary; 03-Jun AMZ-1068/1057/1038/VML-1003-0400/OMDCW). Rules: resolve each shorthand→boonz_products.product_id + product_mapping per machine via docs/refill-aliases.md; pod is WEIMI-fed so LOG-FIRST (log_retroactive_refill_visit, Refill/Add New; WH not debited); Removes via insert_driver_remove_line; combine same product+date+machine (sum qty, earliest expiry, batches in comment). Transfers: AMZ-1057 Krambals Green Olives 1 = [TRANSFER from AMZ-1068 dev0705]. Swaps: NOVO A11 Barkthins Remove→add Be-kind Bar (confirm variant); VML Evian Regular 10 Remove→Popit Mix; AMZ-1038 Organic Rice retired→KitKat/Snack Bar. NOVO Activia Honey&Oats/Strawberry = expiry-replace via adjust_pod_inventory (set, not add). Driver recs→action_tracker(+driver_feedback): AMZ-1038 (more Oreo+McVities); OMDCW (Delice5,Oreo5,McVities Dark4,Milk3,Bueno5,Mars6,Twix4,Nutella4,VW Peach5,Pepsi Diet&Reg6). GAPS — do NOT guess: "Pepsi Peach"@OMDCW no catalog match→park+confirm; MC-2004 Popcorn Butter/Salt dated 01/02/2026 are PAST→skip; "Pepsi Diet&Regular 6"→Pepsi Regular3+Pepsi Black3 (diet=Black rule).

PART 2 — fix 3 dispatch-side bugs, each its OWN migration + Cody, edge cases mandatory:
BUG-C packed-not-in-dispatch (AMZ-1068 VW Reload; AMZ-1057 Pepsi Black+Sunbites; VML all; OMDCW Al Ain Water): RCA the approve→dispatch bridge (approved refill_plan_output rows with NO refill_dispatching row); fix push_plan_to_dispatch/autobridge so every confirmed row bridges. EDGES: manual/edited rows bridge; multi-variant each bridge; source_origin+from_machine_id propagate; a machine confirmed AFTER others in same session still bridges (no cross-machine wipe); idempotent; post-bridge coverage (approved EXCEPT dispatched)=empty.
BUG-D inventory not deducting mid-session/pickup→0 (Al Ain Water shared WH_CENTRAL: AMZ-1068 14 + OMDCW 19; later machines read 0 though stock present; count stuck 37): RCA availability (PRD-017 §1) across sequential same-session confirmations. FIX: decrement exactly once per committed dispatch (no reservation+decrement double-count); product split across machines shows each its fair remainder (never spurious 0 while warehouse_stock>0); count reads live warehouse_stock; never consumer_stock; FEFO. Reconcile Al Ain Water 37-vs-actual in verify.
BUG-E variant packed≠dispatch (Red Bull Regular packed, dispatch shows Diet): multi-variant pod must resolve to the PLANNED/packed variant, not is_global_default; verify packed boonz_product_id == dispatch boonz_product_id per multi-variant row; if ambiguous surface, don't guess. (Outbound sibling of PRD-016 returns variant bug.)

CONSTRAINTS: service-role bypass pattern (IF auth.uid() IS NOT NULL AND NOT role-ok); pod_inventory_audit_log operation lowercase(insert/update/delete)+source∈(seed,sale,refill,manual_edit,weimi_sync,correction,cleanup); pod_inventory.status∈(Active,Inactive,Expired,Removed,Removed/Expired); warehouse_inventory.status manager-only (impersonate WH manager, no service bypass there); RPC-only writes to refill_dispatching/pod_refill_plan/refill_plan_output + add new dispatch writers to enforce_canonical_dispatch_write allow-list; respect block_orphan_internal_transfer+tg_audit_refill_dispatching. Verify each in rolled-back tx; smoke field/packing+field/pickup; update RPC_REGISTRY.md+CHANGELOG.md+PRD-018 status. DONE = Part 1 all logged (transfers/swaps tagged, gaps parked) + BUG-C/D/E fixed+Cody+edge-cases verified + registries updated.
```
