# PRD-073 goal command

GOAL: Execute PRD-073 (docs/prds/PRD-073-empty-shelf-grade-weighted-urgency.md) end to end, AUTO mode. Self-run Dara/Cody. Apply only green pieces, SKIP-and-HIGHLIGHT gate failures, keep PRD-073-EXECUTION-LOG.md current, never wait on CS.

CONTEXT: The data fix already happened from chat on 2026-07-03: 12 Active machines had adyen_inventory_in_store='true' (not 'Live') and were invisible to v_shelf_sales_identity grading, so v_machine_priority read them blind (all grades 0, P3). UPDATE to 'Live' applied and verified (AMZ-1038 and AMZ-1029 went P1 with correct hero grades). Record the 12 machines in the log: AMZ-1029/1038/1057/1068, NOVO-1023, NISSAN-0804, ALJLT-1015, MINDSHARE-1009, ACTIVATE-2005, ACTIVATEMCC-1037, IFLYMCC-1024, MPMCC-1054. Your job is durability + the scoring upgrade.

HARD GATES: engines md5 byte-identical; swaps_enabled false; pick_machines_for_refill body untouched; migrations forward-only; BEGIN..ROLLBACK proof before every write; all new thresholds/weights as columns in pick_urgency_params (single-row, tunable without redeploy).

WS-A eligibility hardening: 1) Find the writer that produced 'true' (suspect the n8n adyen sync writing a boolean; audit n8n flows + any FE/edge writer touching adyen_inventory_in_store) and fix it to write 'Live'/'false'. 2) CHECK constraint on machines.adyen_inventory_in_store allowing the observed legitimate set ('Live','Pending Setup','false','Warehouse Ready','Offline - WH Missing Shelves','Live - WH Storage', NULL), added NOT VALID then VALIDATE. 3) New monitor view v_machine_eligibility_drift: Active + 'Online today' machines contributing zero rows to v_shelf_sales_identity; expect zero rows now. Cody reviews (machines is protected).

WS-B grade-weighted empty/low-fill urgency in v_machine_priority v2: In shelf_graded, add is_empty (stock=0, enabled, not broken) and is_low (0 < fill < low_fill_pct_floor). New machine terms: s_empty = 100*sum(grade_mult over empty)/greatest(total_enabled_shelves,1); s_lowfill same over low shelves. grade_mult from new params empty_wt_a=1.0, empty_wt_b=0.7, empty_wt_c=0.45, empty_wt_d=0.25. Blend adds w_empty (default 0.9) * s_empty + w_lowfill (default 0.5) * s_lowfill. low_fill_pct_floor default 25. New reasons: 'empty_shelves' (s_empty>0), 'low_fill_sellers' (s_lowfill>=20), 'hero_shelf_empty'. P1 escalation: count of EMPTY shelves graded A or B >= p1_empty_ab_min (default 1) forces P1_RESTOCK with 'hero_shelf_empty'. All new columns added to pick_urgency_params with defaults, single row updated.

T-TESTS (log before/after tier tables): T1 AMZ-1038 (A10 Hunter Ridge 0/8, A11 Dubai Popcorn 0/6) is P1 with hero_shelf_empty if either empty slot grades A/B. T2 MC-2004: score rises via empty term; tier moves only if its empty shelf is A/B; log which grade it is. T3 no previously-P1 machine drops tier. T4 fleet tier distribution before vs after; if P1 count more than doubles, reduce w_empty until it does not, and log the tuning; do NOT ship a P1 flood. T5 VOX/main svc_track separation unchanged.

WS-C close: update MIGRATIONS_REGISTRY, RPC_REGISTRY if touched, CHANGELOG, METRICS_REGISTRY (two new terms + params), PRD-073 status line. Commit and push to main; main == origin/main. Final report: what shipped, T-test table, any skips, and the current P1 list with reasons.
