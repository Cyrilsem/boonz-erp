/goal Ship the team report "Stock Transfer Workflow & System Glitches" to GREEN in ONE autonomous run: PRD-050 + PRD-053 + new PRD-056. MODE AUTO. NO pauses, NO CS prompts - run until every acceptance AND edge test passes, then auto-merge each branch to main and deploy FE. Self-run Dara/Cody/Stax.

RAILS (keep even autonomous): forward-only; live bodies via pg_get_functiondef before editing; Cody-review each writer (name articles) without stopping; DEFINERs set app.via_rpc+app.rpc_name + write_audit_log; NEVER write warehouse_inventory.status (Art6); edge+mutating tests in BEGIN..ROLLBACK before apply; NO data loss; engine_add_pod + engine_swap_pod md5 byte-identical; swaps_enabled false; branch per PRD, merge only when that PRD is green.

A) PRD-050 (FE 4->2 bug; report "4 becomes 2"). Spec docs/prds/PRD-050-pickqty-bind-to-plan-not-availability.md. Cap Pick Qty at recommended_qty (plan) NOT batchAvailable; allow over-available with the PRD-045 oversubscribed amber; top-up reaches recommended_qty. Browser 375px T1-T8 (plan4/stock2->4, multi-batch FEFO, save+reload persists 4, a11y). Deploy; smoke VML-1004 Popit = 4.

B) PRD-053 (expiry conservation + per-expiry split; report item 1 + Issue #2 + #3-expiry). Spec docs/prds/PRD-053-stitch-conservation-field-expiry-flagged-adds.md. Execute ALL phases: (A) conservation invariant + publish-time assert - children sum to pod parent, remainder -> expiry NULL row, never cap to pod_inventory; (B) canonical RPC for driver per-expiry split on a dispatch line (reuse p_batch_breakdown shape; SUM=line total, locked) + Stax wires it on dispatching/packing AND the WH returns-approval panel (fix the Ice Tea / multi-expiry return approval that "does not work"); each approved batch -> pod_inventory with its date; (C) flagged additions (needs_review on driver adds beyond plan). Edge: VML Ice Tea REMOVE 13 -> 6+7 not 6; untracked/venue remove fallback.

C) PRD-056 (NEW transfer-aware packing; report Issues #1/#2/#3). FIRST write docs/prds/PRD-056-transfer-aware-packing.md, then build:

1. New pack_outcome 'packed_transferred' (forward CHECK/enum add). For any is_m2m source row, or a row tagged "Transfer from <machine>", the ONLY confirm options are Packed & Transferred (NOT Skip / Not Filled); confirm sets packed=true and writes the unit into the DEST machine's pod_inventory via the existing M2M pairing - no manual notes.
2. Dest auto-tag: the M2M dest leg renders "Transfer from <source>" in refill/packing; one-tap Packed & Transferred confirm.
3. Expiry on transferred adds: adding a transferred product in dispatching exposes an expiry field + per-expiry split rows (reuse B's breakdown); each batch -> pod_inventory with its date on approval.
4. Not-Filled -> transfer link: a Not Filled refill row matched by a transfer added in dispatching links them (overrides Not Filled) with audit trail refill -> transfer -> dispatch -> pod.
   Reuse swap_shelf_pod, convert_removes_to_m2m_transfer, receive_dispatch_line M2M-skip; do NOT duplicate. Dara/Cody backend, Stax FE.

TEST TO GREEN (do not stop): per PRD run all ACs + edges in BEGIN..ROLLBACK, then apply; browser 375px (no h-scroll, >=44px, axe clean). Edge set that MUST pass: a transfer row offers ONLY Packed&Transferred; dest shows Transfer-from + one-tap confirm lands the unit in pod_inventory; a multi-expiry transferred add splits, each batch in pod_inventory with its date; a Not-Filled+transfer pair links with full audit chain; the 4-over-2 line reaches 4 and persists; the conservation assert blocks a leaking fixture; engine add/swap md5 unchanged. Any failure -> fix and re-run; never stop on a failing result.

CLOSE (only at end): auto-merge each green branch to main + deploy FE; set PRD-050/053/056 APPLIED; update CHANGELOG/MIGRATIONS_REGISTRY/RPC_REGISTRY. Final report: per-item verdict table vs the team doc (all green), commits, migrations, 375px screenshots. (HARD SAFETY = the RAILS above, never crossed even autonomous.)
