/goal Ship PRD-121 in ONE loop, end to end, to completion — restore build parity (5 prod-only migrations, 2 local-only branches, dirty tree) and close the machine-status writer gap that left IRIS-1070-0000-O1 trading invisibly for 15 days. Repo: ~/Documents/Boonz Script and Data/BOONZ BRAIN/boonz-erp. Supabase eizcexopcuoycuosittm. Deploy through the existing Vercel pipeline from main.

READ FIRST, IN FULL: docs/prds/PRD-121-build-parity-and-machine-status-writer.md (the spec — §2 evidence, §3 decisions D1–D7 govern), then docs/prds/PRD-119b-goal-command.md and docs/prds/PRD-119b-REPORT.md (the loop that produced the v2/v3 drift — understand how, do not redo any of it), then supabase/migrations/20260824081149_commission_iris_1070_single_door.sql once T1 has reconstructed it (it documents the Article 5 gap you are closing). Registries: RPC_REGISTRY, MIGRATIONS_REGISTRY, METRICS_REGISTRY, CHANGELOG — update all four.

NON-NEGOTIABLE RULES — as PRD-119/119b/120: dara before schema, cody before every migration, canonical RPCs only, SECURITY DEFINER + role check + reason, migration file written and committed BEFORE apply_migration is called (this loop exists because that rule was broken three times in 17 days — if you iterate an RPC, iterate the FILE and re-apply from it; never mint a _v2 through MCP alone), Co-Authored-By trailer, merge to main only with tsc green + fixtures green. Commit directly to main if `git checkout <branch>` is blocked by the permission classifier (same workaround as 119b). Do not touch live plan/dispatch rows. Do not close any stale PRD (D7).

EVIDENCE — verified live 2026-09-08 (full detail in PRD-121 §2)

E1. Five schema_migrations rows with no file: 20260821094253 prd117_reconcile_delivered_consumer_stock, 20260821094348 prd117_1_reconcile_skip_legacy_outcome_rows, 20260824081149 commission_iris_1070_single_door, 20260907053215 prd119b_t1_repair_remove_leg_shelf_lot_rpc_v2, 20260907053259 prd119b_t1_repair_remove_leg_shelf_lot_rpc_v3. Bodies intact in statements[] (1 statement each).
E2. Local-only branches: prd116-fe-dialogs (1 commit ahead: 0fe8f35), backup/pre-rebase-20260831 (8 commits).
E3. Untracked: docs/prds/PRD-119b-goal-command.md. Modified: RPC_REGISTRY.md, PRD-116 ×2, PRD-117-consolidated-remediation.md, src/app/(app)/app/pods/page.tsx, src/app/(field)/field/config/machines/page.tsx.
E4. v_machine_eligibility_drift = IRIS-1070-0000-O1 / 'Switched off' / 'Pending Setup' / 20 sales 7d. machines.updated_at = 2026-08-24 08:12:08 = the commissioning migration, which set status='Active' but never the adyen labels. No automated adyen writer exists anywhere in the repo; machines.status + adyen_status + adyen_inventory_in_store + installation_date have NO canonical RPC; MachineEditPanel writes them by direct table update.

WORK IN ORDER

T1 — Migration parity (D1, D2). For each E1 row: `SELECT array_to_string(statements, E'\n') FROM supabase_migrations.schema_migrations WHERE version = '<v>'`, write byte-for-byte to supabase/migrations/<version>_<name>.sql, prove `md5(file) = md5(remote body)` and print both. One commit: `chore(migrations): reconstruct 5 prod-only migrations from schema_migrations.statements (PRD-121)`. Add the D2 exception (prd118_i_commitment_expose_breakdown → combined file) to MIGRATIONS_REGISTRY. Push.
T2 — Branches (D5). `git cherry origin/main backup/pre-rebase-20260831`: all `-` → delete; any `+` → push the branch and STOP, write it up. Cherry-pick 0fe8f35 onto main, delete prd116-fe-dialogs (local; it has no remote). `boonz_git_cleanup.sh --dry-run` output into the REPORT, then real run. Push.
T3 — Tree (D6). Commit PRD-119b-goal-command.md + the four doc edits (`docs: commit PRD-119b goal command and PRD-116/117 ledger edits`). Diff the two FE files: if they are the 0fe8f35 work, discard after the cherry-pick; if independent, commit separately with a message that says what they are.
T4 — set_machine_status (D3). Load `dara`: signature `set_machine_status(p_machine_id uuid, p_status text, p_adyen_status text, p_adyen_inventory_in_store text, p_installation_date date, p_reason text)`, append-only `machine_status_events`, trigger enforcing `status='Active' AND repurposed_at IS NULL ⇒ adyen_status='Online today' AND adyen_inventory_in_store='Live'`, revoke direct UPDATE on the four columns from authenticated. Load `cody` before the migration. Load `stax`: rewire MachineEditPanel (and any other direct writer found by `grep -rn "adyen_status\|installation_date" src`) to the RPC. Add `age_days` to v_machine_eligibility_drift. Register in RPC_REGISTRY as canonical writer for the four columns. Fixture: a machine flipped to Active without labels → trigger rejects; via RPC → succeeds and writes one event row.
T5 — IRIS-1070 (D4). First real caller of the RPC: `set_machine_status('d5628a72-807a-4a64-9976-5d76139ae354','Active','Online today','Live',NULL,'PRD-121: commissioning 08-24 set status only; adyen labels never flipped')`. Verify: v_machine_eligibility_drift = 0 rows; v_live_shelf_stock.is_eligible_machine = true for its 16 A lanes; `pick_machines_for_refill` dry run for tomorrow lists it as a candidate. Print all three.
T6 — Guards. (a) Nightly assertion in the PRD-119b nightlies family: v_machine_eligibility_drift must be empty, else monitoring_alert with machine + both labels + age_days. (b) scripts/check_migration_parity.sh: remote schema_migrations ≥ 20260615000000 vs supabase/migrations by name substring, exceptions prd053a + prd118_i, non-zero exit on any miss; wire into the existing CI / pre-deploy step so a prod-only migration fails the next deploy instead of waiting for the 07:00 drift check.
T7 — Prove it. `cd "~/Documents/Boonz Script and Data/BOONZ BRAIN" && python3 boonz_build_refresh.py`, parse DATA.git from BOONZ_BUILD_ORCHESTRATOR.html, print main_behind_origin, untracked_migrations, local_only, prunable_count, dirty_count; re-run the E1 name-match and the E4 view. All zero (dirty may show only .claude/settings.local.json) → "Environment GREEN".

STOP CONDITIONS: any `+` from git cherry in T2; any md5 mismatch in T1 (do not "fix" the body — write it up); Cody FAIL after two attempts on T4; any FE write path to the four columns you cannot rewire in T4 (list it, leave the revoke off, write it up).

REPORT — docs/prds/PRD-121-REPORT.md: per item what shipped, Cody verdict, fixtures; the five files with both hashes; the git cherry output; the FE diff decision from T3; T5's three verification outputs; T7's numbers. End with `## PRD-121 DONE` only when T1–T7 are green and the drift check says GREEN.
