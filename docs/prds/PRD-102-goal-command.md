# PRD-102 goal command

Execute PRD-102 (boonz-erp/docs/prds/PRD-102-swap-qty-and-decline.md): pod-swap operator-decided quantity + Don't-swap decline with reason. Supabase prod eizcexopcuoycuosittm. Read the PRD first; it is the spec. Order: Dara → Cody → build → test → ship.

D1 — qty control. Forward-only CREATE OR REPLACE overload: swap_shelf_pod(p_plan_date, p_machine_id, p_shelf_id, p_new_pod_product_id, p_reason, p_new_qty int DEFAULT NULL). NULL = today's fill-to-WEIMI-cap (regression: byte-identical). p_new_qty>0 = spread exactly that qty across WH-available mapped variants via spread_pod_qty largest-remainder mechanics; do NOT clamp to v_shelf_max_stock (old product's cap is meaningless for the incoming product); only WH availability limits — if short, fill what exists and return clamp_reason='wh_limited' + requested_qty. Validate p_new_qty>=1 when given; keep role gate (field_staff, warehouse, operator_admin, superadmin, manager), reason>=10, app.via_rpc/app.rpc_name; all legs still via add_dispatch_row (Article 1). FE packing swap sheet (app/(field)/field/packing/[machineId]/page.tsx): add "Quantity to add" input prefilled with suggested fill (spread_pod_qty total) + show WH-available for the chosen pod; pass p_new_qty.

D2 — decline. New DEFINER decline_swap_pair(p_plan_date, p_machine_id, p_shelf_id, p_dispatch_ids uuid[], p_reason). Role gate MUST include field_staff+warehouse (2026-07-18 role-vocab incident — never reuse driver/warehouse_manager-only lists). Validate: rows exist, match machine/shelf/date, unstarted (filled_quantity=0, not packed/picked_up), reason>=10. Dara decides skipped=true vs include=false (must stay visible as a decision); write edit-log rows edit_kind='decline_swap' (append-only, Article 7). Then INSERT refill_edit_signals signal_type='swap_rejected' (source='field', removed pod/boonz product, machine, reason) — feeds engine_swap_pod R5 cooldown; do NOT edit the engine. FE: "Don't swap" secondary button next to Confirm pod swap (modal + SWAPS pair section) → reason sheet → RPC; declined pair renders struck-through "Declined — [reason]".

Gates: Dara design pass (D2 semantics), then Cody review BOTH migrations (Articles 1,4,7,8,12,13) before apply_migration. Forward-only; no engine edit — Family-A md5 unchanged. Never touch rows past pending/packed. No raw DML on refill_dispatching — RPCs only.

Tests (impersonate via request.jwt.claims; run on WH1-2002-0000-W0 a6c02486-5d95-42ca-9adc-bc755c3019d3 shelf A01 94c292a6-7259-4107-b548-9df03a6be70c, TEST GOOD Dispatch pod bbbbbbbb-0000-0000-0000-000000000002, seeded WH_CENTRAL stock):
T1 NULL qty = today's behavior (fills 8). T2 p_new_qty=6 with cap 8 ⇒ exactly 6 (capacity no longer clamps). T3 p_new_qty=12, WH 8 ⇒ 8 + wh_limited + requested_qty=12. T4 qty 0/negative ⇒ raise. T5 decline as field_staff (bddaec3c-fe18-40db-93e4-8ca543819519) ⇒ legs terminal, edit-log rows, swap_rejected signal row. T6 engine_swap_pod next run does not re-propose (R5 consumed). T7 decline packed pair ⇒ raise. T8 npm run build green; modal shows suggested+WH-available; declined pair struck-through. Clean up test dispatch rows after (remove_dispatch_row).

Close: update CHANGELOG.md, MIGRATIONS_REGISTRY.md, RPC_REGISTRY.md (decline_swap_pair + swap_shelf_pod v2); write PRD-102-EXECUTION-LOG.md; mark PRD SHIPPED; commit+push (main==origin/main). Rollback: forward migration restoring 5-arg body; DROP decline_swap_pair + FE revert; signal rows stay (append-only). If any gate fails or Cody blocks: stop, report verdict verbatim, do not work around.
