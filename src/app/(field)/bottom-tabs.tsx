"use client";

export default function BottomTabs() {
  // Bottom nav removed — home page category cards handle navigation.
  // FieldHeader "← Back" handles back navigation on sub-pages.
  return null;
}
